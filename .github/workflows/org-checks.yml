name: Org Checks

on:
  pull_request_review:

jobs:
  check-approvals:
    name: Check for Team/Admin Review
    uses: immich-app/devtools/workflows/required-approval.yml@feat/github-security-hardening
    permissions:
      pull-requests: read
      contents: read
